<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebasetask1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pojo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InsertingMultipleValues">

        </activity>

        <activity android:name=".ReadWriteListOfChilds">

        </activity>
        <activity android:name=".DeleteData" />
        <activity android:name=".UpdatedDataa" />
        <activity android:name=".PushIdd" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>